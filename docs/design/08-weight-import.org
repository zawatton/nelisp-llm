#+TITLE: Weight import -- run an open-weight Qwen model natively, without training it
#+OPTIONS: toc:2 num:t
#+STATUS: P0-P2d SHIPPED; P3 -- the loop runs and is verified; CPU cost is the limit

* Status
Phases 0, 1, 2a, 2b and 2c SHIPPED on the CPU -- an imported Qwen3-0.6B runs,
matches a reference to 1e-14 through all 28 layers, and predicts the donor's
next token exactly.  Phase 2d SHIPPED: every linear runs on the GPU, a block 57x
faster than the Elisp loop, and the whole 28-layer stack predicts the same
token the CPU oracle does.  Phase 3: the distillation data path works against a live
teacher, a LoRA trains over the frozen int8 base, a gradient runs through a
whole block, and the completion-only training loop is verified end to end.  What
limits it now is measured rather than unknown: 327s per step at seq 1 on the
CPU, which the GPU's 58x on the forward linears already halves the fix for -- a
transposed DP4A kernel is what remains.

*Phase 0* (a locally served donor as teacher and as an immediately usable
model): =examples/ollama-provider.el= + =make ollama-provider=.  No change was
needed in =nl-llm-agent-openai.el= -- it reads only the =content= field, so a
reasoning donor's chain of thought (returned separately as =reasoning=) is
discarded and the final answer is what reaches the agent.  Measured against
=qwen3:4b= served by Ollama on this host: 2/2 exact-answer cases, 1.2s and
14.3s.  One trap found and guarded in the example: a completion truncated while
the model is still reasoning arrives as an *empty string*, not an error, so an
empty completion must be rejected rather than used as a training target.

Ordering note: the Phase 0 example needs =lisp/nl-llm-agent-provider.el= and
=lisp/nl-llm-agent-openai.el=, which belong to the agent-harness work of Doc 5
and are not part of this change.  =make ollama-provider= therefore fails with a
missing library until that layer lands; Phase 1 has no such dependency and runs
on its own.

*Phase 1* (donor token-id parity): =lisp/nl-llm-qwen-tokenizer.el= +
=tools/qwen-tokenizer-export.py= + =tools/qwen-tokenizer-fixtures.py= +
=test/qwen-tokenizer-test.el= (=make qwen-tokenizer-table=, then =make
test-qwen-tokenizer=).  Against Qwen3-0.6B's own =tokenizer.json=:

| check                                  | result                        |
|----------------------------------------+-------------------------------|
| 71 hand-picked cases, exact id equality | 71/71                         |
| 4 whole corpora, exact id equality      | 3869 ids, 4/4 files           |
| decode-then-encode stability            | all cases                     |
| byte alphabet complete                  | 256/256                       |
| pinned categories == reference          | 17569 code points             |
| pre-tokenizer pieces rejoin to input    | all cases                     |
| negative control (broken merge)         | detected                      |
| negative control (stale fixture digest) | detected                      |
| table load                              | 151387 merges, ~0.5s          |
| encode throughput                       | ~30-40k chars/s (Emacs 30)    |

The corpora are English open data, Elisp source, org markup with tables, and
Japanese technical prose -- i.e. what this model is actually going to be fed.

Four findings worth carrying forward, all of which were silent failures before
they were caught:

- =compare-strings= returns =t= on equality, not =0=.  The added-token matcher
  tested for =0= and therefore never matched, so =<|im_start|>= tokenized as
  seven ordinary tokens.  Nothing errored; the ids were simply wrong.
- =encode-coding-string= / =decode-coding-string= with plain =utf-8= apply EOL
  conversion, silently folding CRLF to LF.  Both directions must pin
  =utf-8-unix=, or any Windows-authored input shifts ids.
- Deriving the byte -> token map by scanning for single-byte tokens (a
  bijection, asserted at load with a 256/256 check) removes an exported table
  that could disagree with the blob.  Cheaper to verify than to trust.
- *The Unicode tables are a hidden host dependency.*  The pre-tokenizer needs
  \p{L} and \p{N}, and the first implementation read them from Emacs with
  =get-char-code-property=.  That passes every parity test and is still wrong:
  the reference library tracks its own Unicode version, so a code point one
  side has not heard of yet tokenizes differently, with no error anywhere.
  Measured on this host, three sources disagree about which code points are
  letters -- Python's =unicodedata= 15.1 differs from the reference on 4382
  code points, and this Emacs differs on 24 of the 17569 sampled.  The fix is
  to *probe the reference pre-tokenizer itself*, one code point at a time
  (=pre_tokenize_str("a" + X)= is one piece exactly when X is a letter) and
  ship the resulting ranges inside the table.  A full 1.1M-code-point sweep
  costs about 3 seconds, the ranges add 6 KB, and token ids stop depending on
  the host -- which is also what lets the tokenizer run where
  =get-char-code-property= does not exist.

Portability, measured against the NeLisp standalone binary: =cl-lib=,
=make-hash-table=, =encode-coding-string= / =decode-coding-string=,
=insert-file-contents-literally=, =set-buffer-multibyte=, =compare-strings=,
=string-to-vector=, =cl-subseq= and =sort= are all present, so the loader, the
pre-tokenizer scanner and the BPE itself run there.  Two gaps remain, both at
the encode entry point rather than in the algorithm: =ucs-normalize-NFC-string=
(the NFC step, which =nl-llm-qwen-tok-encode= refuses to skip silently) and
=secure-hash= (used only by the test's staleness guard).  Pinning the category
ranges removed the third gap, =get-char-code-property=.


*Phase 2a* (decoupled head dimension) SHIPPED -- =lisp/nl-llm-attn.el=,
=nl-llm-block.el= and =nl-llm-decode.el= now take the per-head width from
=:head-dim= on the layer (or the model, for blocks that do not state their own),
falling back to =(/ dim heads)= so every existing model and test is unaffected.
=test/head-dim-test.el= + =make test-head-dim= (13/13).

The suite was calibrated against the unmodified code first, and the calibration
is the interesting part.  Before the change, with a Qwen-like shape:

| check                                  | before  | after   |
|----------------------------------------+---------+---------|
| coupled head dim == reference          | PASS    | PASS    |
| decoupled head dim == reference        | *FAIL*  | PASS    |
| decoupled output shape is (seq x dim)  | PASS    | PASS    |
| decoupled cached == full forward       | *FAIL*  | PASS    |

The shape check passing while the numbers were wrong by 0.74 is the whole
problem in one line: the projections are the right shape, so nothing objects --
the loops simply stride =(/ dim heads)= through a =heads * head_dim= wide query
row.  That is why the suite carries its *own* reference implementation of
decoupled attention instead of comparing two nelisp-llm paths: both paths shared
the assumption, so they would have agreed while both were wrong.  (They did not,
as it turned out, which is why the cached-vs-full row is also red before -- but
that was luck, not a designed detector.)

Only those three modules implement a decoupled width.  The others still derive
it, so rather than ignore the key they refuse it, via
=nl-llm-attn-reject-decoupled-head-dim=: =nl-llm-stream-block=,
=nl-llm-integrated--blk=, =nl-llm-ag-block= and =nl-llm-gpu--decode-block=
signal instead of returning plausible numbers.  Each guard is exercised in the
suite except the GPU one, which needs a Vulkan device and which Phase 2b
migrates rather than guards.  The contract is: a path either implements
=:head-dim= or errors on it.

One regression was caught by the existing suites and is worth recording.
=nl-llm--dcache-validate-layout= required =dim= to divide =heads=, and relaxing
that for caches carrying an explicit head width looked obviously safe -- a
decoupled model does not need the relationship.  But a head width is now
*always* stored, so the condition switched the check off for every cache, and
=test/decode-capacity-test.el= went red on a malformed-cache case that had been
rejected before.  The check is unconditional again: every Qwen3 dense size
satisfies it anyway (1024/16, 2048/16, 2560/32), so there was nothing to relax
it for.

Pre-existing and untouched: =nl-llm-stream.el=, =nl-llm-integrated.el= and
=nl-llm-gpu-decode.el= already fail =byte-compile-error-on-warn= (two
over-long docstrings and one unused argument).  Confirmed identical at the
parent commit before blaming the refactor.

*Phase 2b* (offline converter) SHIPPED -- =tools/qwen-weights-export.py= +
=tools/qwen-weights-verify.py= + =test/weights-header-test.el=
(=make qwen-weights-table=, =make verify-qwen-weights=, =make
test-weights-header=).  Qwen3-0.6B's 1434 MiB of bf16 becomes a 571 MiB
=nl-llm-wts-v1= table: 310 tensors, 596.0M parameters, 568 MiB of int8 payload
and 0.25 MiB of f32, with a 64 KiB header.

| measurement                                   | result                |
|-----------------------------------------------+-----------------------|
| packing round trip, every int8 tensor         | bit-exact             |
| padding lanes nonzero                         | 0 of 197 tensors      |
| worst per-tensor relative dequantization error | 0.0039 (0.39%)        |
| donor tensors mapped                          | 311 of 311            |
| header readable by Emacs =read=               | yes, 15/15 checks     |
| conversion wall time                          | ~6s                   |

Quantization is per-output-row int8, not per-tensor: one scale is set by the
largest row and wastes most of the range on every other one.  RMSNorm gains and
the Qwen3 =q_norm= / =k_norm= vectors stay f32 -- a few thousand floats in total,
and the tensors quantization error hurts most.  The 568 MiB of weights leaves
the 6 GB card most of its memory for the KV cache, so the Doc-01 ternary path is
not needed for this size and should not be used (it assumes QAT).

The header is *one Lisp sexp*, not JSON, so the Phase 2c loader needs no parser
and does not depend on the JSON differences between Emacs and NeLisp.
=test/weights-header-test.el= pins that: =read= accepts it, the config values
round-trip as integers and floats matching =config.json=, every declared tensor
extent lies inside the file, int8 byte counts match rows x words x 4, and every
role the model plist needs is present.

Three things the donor turned out to disagree with the design about:

- *=lm_head.weight= is present despite =tie_word_embeddings: true=*, and is
  bitwise identical to =model.embed_tokens.weight=.  Storing both would cost
  155 MiB of int8 payload for a duplicate.  The converter drops it -- but only
  after proving equality; a donor whose head actually differs under that flag
  errors out and says the config is not the whole story.
- *The DP4A kernel is closer to per-channel scaling than assumed -- but the
  one-token change was wrong.*  =bitlinear-dp4a-1f= reads =(aref BETA 0)=, a
  single scalar weight scale, and =o= (the output row) is already in scope on
  that line, so changing =0= to =o= looked free.  It is not: =nl-llm-bitnet.el=
  calls that kernel in two places and passes BETA as =(vector beta)=, a
  *one-element* buffer, so re-indexing it would have the ternary path read past
  the end.  Corrected in Phase 2d by adding a sibling kernel with per-row BETA
  and leaving =bitlinear-dp4a-1f= alone.  Recorded rather than quietly fixed:
  "already in scope" said nothing about who else was calling it, and checking
  the callers is what turned a one-token change into a new kernel.
- *=head_dim= 128 with =dim= 1024 and 16 heads is exactly what Phase 2a made
  representable*, confirmed by the real tensors: =q_proj= is [2048, 1024],
  =o_proj= is [1024, 2048], =k_proj= / =v_proj= are [1024, 1024] = 8 x 128.  The
  header carries =:head-dim= and the suite asserts it differs from
  =(/ dim heads)=, so a loader cannot quietly fall back.

Both verifiers are calibrated.  =qwen-weights-verify.py= was run against a table
with one payload byte flipped (relative error 0.35, red) and one scale
overwritten (398520, red), returning to OK after restore; it also parses the
header through cruder string handling than the writer used, so a malformed sexp
fails there as well as in Emacs.  =test/weights-header-test.el= was run against
a header whose =:nbytes= had been inflated past the end of the file (red).  Both
skip cleanly when the donor table is absent, so a fresh clone is not red.

*Phase 2c* (Elisp reader) PARTLY SHIPPED -- =lisp/nl-llm-weights.el= +
=tools/qwen-weights-rows.py= + =test/weights-load-test.el= (10/10,
=make test-weights-load=).  The reader half is done; bulk upload to a resident
GPU buffer is not, for a reason worth stating (below).

What the reader does *not* do is the design constraint.  Nothing in it builds a
full-precision vector for a large tensor: a tensor is addressed by byte range
and handed out either as raw bytes -- already the u32 words the DP4A kernel
reads through =bitcast-u=, so an uploader never touches a float -- or one
dequantized row at a time.  Opening the 598 MB table costs 0.00s because only
the 64 KiB header is read, and =nl-llm-weights-f32-tensor= *refuses* an int8
tensor rather than quietly materializing 14 GB of boxed floats; that refusal is
a check in the suite, not a comment.

Row-at-a-time is also what makes the import checkable.  Three things can be
wrong while still producing numbers -- which lane of a packed word is read,
which per-row scale is paired with a row, and how four bytes become a float --
so each is compared separately against =tools/qwen-weights-rows.py=:

| comparison                              | result                     |
|-----------------------------------------+----------------------------|
| unpacked int8 lanes == exporter          | 17 rows, exact             |
| per-output-row scales == exporter        | exact                      |
| dequantized values == exporter           | 18688 values, exact (f64)  |
| raw byte extent == header                | 2097152 = 2048 x 256 x 4   |
| int8 wholesale dequantization            | refused                    |
| row index out of range                   | signals                    |

The reference products are computed in *float64*, not float32.  =int8 *
f32_scale= can need more than 24 mantissa bits, so a float32 reference would
disagree with Emacs's arithmetic and force a tolerance -- and a tolerance is
exactly the slack an off-by-one lane index hides in.  In float64 both sides are
bit-identical and the comparison has no give.  (The GPU multiplies in f32; that
is Phase 2d's concern.)  Calibrated by flipping one payload byte (lanes and
values both red, proving the reader really reads the file) and by perturbing one
fixture scale in its last decimal place (scales red, proving the comparison is
live).

*Why the upload is not here.*  =nelisp-gpu-server-upload-u32= already stores
words verbatim for exactly this packing, but it takes a *vector of Elisp
integers* and does =(append uvec nil)= before encoding.  For this payload that is
~149M words: a 1.2 GB vector plus a 2.4 GB list, to send bytes that are already
correctly laid out on disk.  The missing primitive is an upload that takes a
unibyte string, which is a ~10-line addition on the nelisp-gpu side and turns
the whole transfer into one read plus one write per tensor.

That addition lands in the *Notes* repository, not this one -- =dev/nelisp-gpu=
is a directory of Notes rather than a repository of its own, and Notes currently
carries unrelated uncommitted work.  So it is deliberately a separate change
with its own commit scope, not smuggled in here.

*Phase 3* (adaptation) STARTED -- the data path and the trainable adapter both
exist; wiring the adapter into a whole block is what remains.

*Distillation data* -- =lisp/nl-llm-distill.el= + =test/distill-test.el= (14/14,
=make test-distill=) + =examples/distill-from-teacher.el= (=make distill=).
Turns a locally served teacher's answers into the vector of (:prompt
:completion) that =nl-llm-agent-supervised= takes.  The teacher is passed as a
*function*, not a URL: a donor must be locally-run open weights, outputs from a
hosted API may not be used to train a model, and the difference is which
endpoint was called -- something the caller knows and no gate can detect.

The gates are the substance.  Each is a failure seen rather than a precaution:
=empty= (a reasoning model truncated mid-thought returns "", which Phase 0
found), =echo= (small instruct models hand the prompt back), =duplicate=,
=too-long=, =budget=, =over-count=, and =error= for a teacher that signals.
Every rejection is counted by reason, because a run that accepted 3 of 200
prompts is a broken teacher and that must be visible without opening the output.

A real run bears this out.  Ten prompts against =qwen3:4b= gave *9 accepted, 1
rejected (empty 1)* -- the reasoning-truncation gate, built from a Phase 0
observation, firing on live data.  The dataset then passed its actual consumer:
=nl-llm-agent-supervised--prepare= accepted 9 examples, 11547 completion tokens,
loss starting at (57 50 47 58 39 61 31 60 62), i.e. exactly where each prompt
ends.

*The barrier to training, removed* -- =lisp/nl-llm-weights-lora.el= +
=test/weights-lora-test.el= (11/11, =make test-weights-lora=).  A frozen
quantized weight still has to appear in the backward pass, because
=dL/dx = W^T.g= even when W collects no gradient of its own -- "frozen" means
untrained, not unused.  =nl-llm-weights-apply-t= supplies that term straight off
the int8 table, so a dequantized base's 14 GB of boxed floats are avoided in
both directions.

| check                                     | result                    |
|-------------------------------------------+---------------------------|
| <W.x, g> = <x, W^T.g>                     | rel 1.5e-16               |
| dL/dA vs finite differences               | worst rel 6.5e-08         |
| dL/dB vs finite differences               | worst rel 4.2e-08         |
| dL/dx vs finite differences (needs W^T)   | worst rel 5.2e-07         |
| *control: dL/dx with W^T dropped*         | *wrong by rel 1.59*       |
| fresh adapter (B = 0) vs base             | bit-identical             |
| trains                                    | loss 0.0127 -> 0.0000     |
| base after training                       | bit-identical             |

The transpose gets the inner-product identity rather than a spot check because a
transposed loop reads almost exactly like the forward one, so reading it proves
nothing and no index swap survives the identity.  The control is the number that
makes the case: drop =W^T.g= and =dL/dx= is wrong by 159%, which is what "the
frozen base participates in the backward" means quantitatively.

One thing the suite got wrong first, worth keeping: the training check initially
fed inputs around +-39, the loss started at 1.1e7 and plain SGD diverged to NaN
in sixty steps -- with gradients already verified correct.  That was the test's
data, not the code, and it is also the honest shape of =nl-llm-wlora-fit=, which
is unclipped SGD and says so now.  Real adaptation wants the Adam and clipping
=nl-llm-gpu-ag.el= already has for the f32 path.

*The pieces between the linears* -- =lisp/nl-llm-weights-backward.el= +
=test/weights-backward-test.el= (12/12, =make test-weights-backward=).  Every
vjp a block needs, each checked against finite differences on its own:

| vjp                       | worst rel vs central differences |
|---------------------------+----------------------------------|
| RMSNorm                   | 1.9e-07                          |
| RoPE, interleaved         | 1.8e-08                          |
| RoPE, half-split          | 1.0e-08                          |
| QK-norm                   | 1.4e-07                          |
| SwiGLU, d/dgate           | 3.7e-07                          |
| SwiGLU, d/dup             | 5.5e-07                          |
| attention, d/dq d/dk d/dv | agreement to 5e-10 absolute      |

Individually before composing, deliberately.  A composed check is stronger --
a correct gradient through a whole block implies every vjp inside it -- but when
it fails it says only "somewhere in here", and these are functions where a sign
or a missing term hides while still producing something that looks like a
descent direction.  Two controls put numbers on that: dropping RMSNorm's
non-diagonal mean term makes its gradient wrong by 417x, and dropping the
softmax Jacobian's =- <p, dp>= subtraction makes d/dq wrong by 7e8 while
leaving d/dv untouched -- so a suite that checked only d/dv would pass.

A third finding about scale, after the LoRA divergence and now this.  The
attention check first reported d/dq wrong at rel 1.4e-03.  It was not: at the
input scale used, one head's scores reached 22.4 against -10.1, a spread of
32.5, so the softmax saturated and the true gradient was *-1.4e-12*.  A
relative comparison against a central difference at 1e-6 was measuring
rounding.  Fixed two ways -- inputs at a scale where scores are O(1), which is
where RMSNorm and QK-norm actually put them, and a checker that passes on
either small relative *or* small absolute disagreement, since a legitimately
tiny gradient cannot be judged relatively.  The pattern worth carrying: when
gradients are already verified and something still looks wrong, suspect the
input scale and the metric before the formula.

*A gradient through a whole block* -- =nl-llm-wb-block-forward= /
=nl-llm-wb-block-backward= + =test/weights-block-backward-test.el= (17/17,
=make test-block-backward=).  The composition, which is the check that matters
and the one that could not have been written first: when it fails it says only
"somewhere in here".

| check                                          | result              |
|------------------------------------------------+---------------------|
| taped forward == =nl-llm-wf-block=             | *exactly*, maxdiff 0 |
| dL/dA through the block, LoRA on each of 7 roles | worst rel 2.2e-06  |
| dL/dB through the block, same                  | worst rel 1.2e-06   |
| dL/dx through the block                        | rel 1.1e-08         |

The forward needed a tape, and it is a separate function from
=nl-llm-wf-block= rather than a flag on it, because that one is the oracle the
whole import is verified against to 1e-14 and is not worth disturbing.  The
suite pins them together instead: a forward that drifted while gaining a tape
would leave every gradient correct for the wrong function.

Two values on the tape are easy to get wrong and unrecoverable afterwards:
QK-norm's vjp needs q and k as they were *before* normalisation, attention's
needs them *after* the rotation.  Both are saved, under names that say which.
=seq= is 2 in the suite on purpose -- at 1 a position attends only to itself and
attention's cross-position dq/dk terms are never exercised -- and the test layer
has =heads*head_dim= 12 against =dim= 8, so the decoupled width is live too.

One bug this found, of a kind worth naming: the first tape filled both =:g= and
=:fg= with =(nreverse gs)=, calling a destructive operation twice on one list
and corrupting it.  Caught before it ran, but the shape of the mistake --
reaching for the same accumulator twice because two fields seemed to want it --
is why the tape now keeps one list per thing and the saved triples are the only
copy.

*The loop* -- =lisp/nl-llm-weights-train.el= + =test/weights-train-test.el=
(11/11, =make test-train=).  Completion-only cross-entropy over an imported
model: encode with the *donor's* tokenizer, run the stack with tapes, score the
positions whose next token is a completion token, and push the gradient back
through the head and every block into the LoRA.

The donor's tokenizer, not the agent's.  =nl-llm-agent-supervised= uses the
bounded ascii/utf8 tokenizers, which is right for a model trained from scratch
beside them and wrong here: an imported model's embedding rows are indexed by
Qwen's 151936-entry vocabulary, so training against any other id space is
training against noise.  The suite pins that against the ids from the Phase 2c
milestone -- "The capital of France is" + " Paris" encodes to
(785 6722 315 9625 374 12095) with the boundary at 5.

| check                                         | result                  |
|-----------------------------------------------+-------------------------|
| dL/dA through head + 2 blocks + loss          | agreement to 6e-10      |
| dL/dB through head + 2 blocks + loss          | agreement to 5e-10      |
| changing a completion target changes the loss | 2.7083 -> 2.6987        |
| a boundary with no completion positions       | signals                 |
| loss is a mean, not a sum                     | 2.71 vs ln 11 = 2.40    |
| a step descends                               | 4.03 -> 1.82, 25 steps  |

*And the honest cost.*  Measured on the real Qwen3-0.6B, one block at one
position: forward 6.74s, backward 4.92s.  So a single training step over 28
layers at *seq 1* is 327s -- five and a half minutes -- and a realistic example
is tens to hundreds of tokens, which puts a CPU step in hours.  The loop is
correct; it is not yet practical on this path, and that is a measurement rather
than an impression.

Two things that measurement says beyond the headline.  The backward is
*cheaper* than the forward, ratio 0.73, because it is seven transposes against
the forward's seven multiplies plus the adapter, and the transpose skips rows
whose scale times gradient is zero.  And a rank-8 LoRA on one role is 16384
trainable parameters per layer, which was never the part that cost anything.

*The transpose is now on the GPU too* -- =dp4a-rows-t= in nelisp-gpu, wrapped by
=nl-llm-wgpu-apply-t=.  Measured on a real 2048 x 1024 imported weight:

| measurement                            | result        |
|----------------------------------------+---------------|
| GPU vs the Elisp loop                  | 0.0124s vs 0.8315s, 67x |
| GPU transpose vs the CPU transpose     | rel 1.6e-06   |
| <W.x, g> = <x, W^T.g>, W^T on the GPU  | rel 5.2e-07   |

One thread per input column, which is the awkward direction -- column i lives in
word i/4, lane i%4 of every row, so a thread strides down the weight rather than
along it.  DP4A does not apply despite the layout: BETA varies per row and
multiplies each term, so it cannot be factored out of an integer dot product the
way the forward's single accumulation can.  The sum is float, which is why this
is only as tight as f32 against a float64 reference instead of the forward's
8e-08.  A DP4A variant would need the gradient quantized as well, and a second,
looser comparison to go with it.

The inner-product identity carries the structural weight there: it needs no
reference implementation at all, and no index error survives it -- which matters
for the one access pattern that is easy to get backwards.

*And the block backward is wired through it* -- =nl-llm-wgpu-with-transposes=
binds =nl-llm-wb-transpose-fn=, so every frozen base's W^T.g inside
=nl-llm-wb-block-backward= goes to the GPU without that file knowing there is
one.  A dynamic binding rather than an argument threaded through six call
sites, and unbound it restores the CPU path exactly, which is what keeps the
verified reference available to compare against.

| measurement                              | result              |
|------------------------------------------+---------------------|
| block backward, GPU vs CPU               | 0.129s vs 4.85s, 37x |
| dx, GPU vs CPU                           | rel 1.1e-06         |
| the adapter's dA / dB, GPU vs CPU        | rel 8.5e-07 / 1.4e-06 |
| a linear absent from the table           | falls back, identical |

The handles are keyed by the linear *object*, and the upload takes the CPU
layer's own linears rather than a fresh load, because two loads of one tensor
are equal in content and not =eq= -- a lookup that missed would fall back to
the CPU silently and look merely slow.

One claim corrected here.  The suite first asserted that the adapter's dA and
dB would be *bit-identical* under the routing, on the reasoning that they are
built from the adapter's own arithmetic and never touch W^T.  A probe appeared
to confirm it at rel 0.000e+00 -- but only because that probe left B at zero,
which makes dA identically zero on both sides.  The moment B was perturbed the
check failed, correctly: dA and dB do not touch their *own* transpose, but for a
LoRA on =:wv= the gradient arriving there has already come back through =:wo='s
transpose and the attention.  Everything downstream of a transpose inherits its
f32 accumulation.

So a training step's arithmetic is now GPU-side in both directions: the forward
linears at 57x per block, the backward at 37x.  What remains between that and a
practical loop is the upload, which this version pays per block per step, and
the head.

Independent and still open: the f32 autograd path's own migration, since
=nl-llm-ag-block= *refuses* =:head-dim=, =:rope-style= and QK-norm.  That
refusal is the guard working correctly, and it blocks training a Qwen-shaped
model through =photon-autograd= -- not the int8 path above, which has its own
forward and backward.

*Phase 2d* (GPU) STARTED -- one imported linear now runs on the GPU.
=lisp/nl-llm-weights-gpu.el= + =test/weights-gpu-test.el= (7/7,
=make test-weights-gpu=), plus two additions on the =nelisp-gpu= side
(=nelisp-gpu-server-upload-bytes= and the =bitlinear-dp4a-rows= kernel).

*Those two additions are not under version control.*  Earlier notes here said
they belonged to the Notes repository and would be committed separately; that
was wrong.  Notes ignores =dev/*= with the comment that each dev project is its
own repository, and =nelisp-photon=, =nelisp-llm=, =nelisp= and =nelisp-agent=
all are -- but =nelisp-gpu= has no =.git= at all.  So its whole source, the
Elisp client and the C =vkserver=, exists on one machine with no history and no
backup, and the changes above are only on disk.  That wants deciding before more
of Phase 2d lands on top of it.

| measurement                        | result                        |
|------------------------------------+-------------------------------|
| 2 MiB weight payload uploaded      | as bytes, no float built      |
| GPU vs the same W8A8 on the CPU    | rel 8.2e-08 (f32 accumulate)  |
| activation quantization cost       | rel 6.7e-03                   |
| GPU vs the Elisp loop, one linear  | 0.016s vs 1.048s (67x)        |
| one whole block, GPU vs CPU f32    | 0.116s vs 6.58s (57x)         |
| one whole block, GPU vs CPU W8A8   | rel 3.2e-08                   |
| control: one row scale doubled     | detected                      |

*The acceptance criterion is met.*  Running all 28 layers with every linear on
the GPU predicts *the same token the CPU oracle predicted*:

| path                 | token          | logit     | time  |
|----------------------+----------------+-----------+-------|
| CPU oracle, f32 act  | 12095 " Paris" | 17.189348 | 1025s |
| GPU, W8A8            | 12095 " Paris" | 16.324242 |  210s |

That was not a formality.  Activation quantization costs about 0.7% per block,
so whether the argmax survives twenty-eight of them is a question, and the
answer is that it does -- while the logit moves by 5%.  Worth stating both ways
round: the ranking is preserved, the value is not, so anything that reads the
logit rather than the argmax (a temperature sample, a confidence threshold)
needs measuring before it is trusted on this path.

The 4.9x end-to-end speedup is smaller than the 57x per block because compute
is no longer where the time goes.  Of 210s, about 140s is uploading weights
(28 layers x ~5s, since this version loads and frees one layer at a time to
keep VRAM at one block) and about 61s is the tied head, still on the CPU.  The
28 blocks of actual GPU arithmetic are ~17s.  So the next two wins are known
and neither needs new numerics: keep all 568 MB resident, which fits 6 GB with
room to spare and makes the upload a one-time cost, and move the head to the
same kernel.

=make test-weights-gpu= runs the fast checks in about ten seconds;
=NL_LLM_GPU_E2E=1 make test-weights-gpu= adds the four-minute end-to-end check
that pins the 12095 above.

The bytes go from =insert-file-contents-literally= to the GPU buffer untouched,
because the table's payload is already the uint32 words of four int8 lanes the
kernel reads back with =bitcast-u=.  That is what
=nelisp-gpu-server-upload-bytes= is for; =upload-u32= would have built a 1.2 GB
vector and a 2.4 GB list for a model's worth of words.

Two checks, because one cannot answer both questions.
=nl-llm-weights-apply-w8a8= reproduces the kernel's exact arithmetic on the CPU
-- integer lane products, scaled once by the weight row's scale times the
activation's -- so "did the transfer and kernel do the right thing" gets a tight
threshold, while "what does quantizing the activation cost" is a number to
report.  Comparing the GPU against the f32-activation path would have conflated
them at 6.7e-03 and called it agreement.

One trap worth recording: the first measurement said activation quantization
costs *nothing*, rel 0.000e+00.  The input was a raw embedding row, which is
=lane * scale= and therefore already exactly int8-representable, so
re-quantizing it is lossless.  The suite now uses a post-RMSNorm activation --
what a linear actually sees -- and *asserts that the activation is not
int8-exact*, so the number cannot quietly go back to measuring nothing.

*The imported model runs.*  =lisp/nl-llm-weights-forward.el= +
=tools/qwen-forward-ref.py= + =tools/nlwts.py= + =test/weights-forward-test.el=
(7/7, =make test-weights-forward=).  Pure Elisp, straight off the int8 table,
compared against a numpy reference *layer by layer*:

| state           | relative error vs reference |
|-----------------+-----------------------------|
| embedding       | 0.00e+00                    |
| after layer 0   | 1.99e-15                    |
| after layer 1   | 2.03e-15                    |

That is float64 round-off -- summation order, nothing else.  The reference reads
the *same* int8 table and dequantizes it the same way, so a difference cannot be
blamed on quantization; it isolates the arithmetic.  Logits at the last position
come out sensible too (token 14990 "hello" scores 6.43, token 100 scores -3.88),
with only the requested head rows read, since scoring all 151936 would cost more
than the rest of the forward.

Layer by layer rather than on the logits, because the logits only say "wrong".
The calibration shows why that matters: running the same forward with the
*interleaved* rotation instead of half-split leaves the embedding exact and
turns layer 0 red at relative 2.5e-1, naming the layer where the convention is
wrong.  Perturbing one reference value instead reddens the embedding.  Both
controls were run and both trip; the suite skips when the donor table is absent.

Cost: about 6.5 s per layer per token, so roughly three minutes per token for
all 28 layers.  That is deliberate.  Weights stay int8 and are accumulated lane
by lane in the same order the DP4A kernel uses, one tensor is read per linear
per layer (7 reads instead of 12288 row fetches, ~12 MB resident per layer), and
the attention loop is written out rather than routed through =nl-llm-gqa=, which
wants photon-tensors this model cannot afford.  That duplication is the risk, so
the suite also pins the loop against the definition on a synthetic case small
enough for both -- a check that needs no donor and runs in milliseconds.

*Two more silent convention mismatches*, found while building toward a
reference forward and fixed in =lisp/nl-llm-attn.el= with
=test/rope-style-test.el= (10/10, =make test-rope-style=):

- *RoPE pairing.*  =nl-llm--rope-block= rotates adjacent pairs (2m, 2m+1), the
  GPT-J convention.  Qwen3 and Llama rotate half-split pairs (i, i + hd/2), the
  GPT-NeoX convention HuggingFace calls =rotate_half=.  Same angles, different
  pairing.  Measured before assuming: an 8-wide head at position 3 differs by
  *5.82* between the two.  Nothing raises, because the vector is the right
  length either way.  Now selected by =:rope-style=, `interleaved' (unchanged
  default) or `half'; an unrecognised name signals rather than falling back.
- *QK-norm.*  Qwen3 RMSNorms each head's q and k with a learned per-head-width
  gain *before* the rotation.  Omitted, the attention scores are simply scaled
  wrong.  Now applied from =:q-norm= / =:k-norm= on the layer, nil being a
  no-op so a donor without them behaves exactly as before.  The exporter
  already carries both tensors as f32 (=q_norm= / =k_norm=, 128 wide).

That is three conventions in this family -- =:head-dim=, =:rope-style=,
=:q-norm= / =:k-norm= -- each of which produces confident wrong numbers when
ignored, and none of which the shapes catch.  They share one contract:
=nl-llm-attn-reject-decoupled-head-dim= refuses all three in the paths that do
not implement them (stream, integrated, autograd, GPU decode), so a forgotten
migration is an error.  The suite's calibration made the point plainly: before
the change, "the two rope styles differ" failed with *maxdiff 0.000* -- the key
was not merely mishandled, it was read by nothing at all.

Head-dimension finding that resizes Phase 2: Qwen3-0.6B's =config.json= has
=hidden_size= 1024, =num_attention_heads= 16 and =head_dim= 128, so
=q_proj= is 1024 -> 2048.  =nl-llm-attn.el= derives =hd= as =(/ dim heads)= at
every call site, which would give 64.  The decoupled =:head-dim= refactor is
therefore confirmed as the largest item in Phase 2, not a hypothetical.

The goal is to skip pre-training entirely: take an Apache-2.0 open-weight
checkpoint (Qwen3 dense), convert it into a nelisp-llm model, and run it on the
existing =nelisp-gpu= substrate.  Personal-task adaptation then happens through
LoRA (=lisp/nl-llm-lora*.el=) on data distilled from a larger teacher, not
through pre-training.

Cost argument for doing this at all: reaching usable quality from scratch at
100M parameters needs roughly 2e9 tokens at the Chinchilla 20:1 ratio, i.e.
=6*N*D= ~ 1.2e18 FLOP.  On a GTX 1060 6GB driven from Elisp that is not a
weeks-long job, it is an out-of-reach job.  Importing costs 0 training FLOP and
starts at the donor model's quality; a rank-8 LoRA over a 0.6B base trains ~2M
parameters and is an hours-long job on the same hardware.  The ratio is three to
five orders of magnitude.

* Motivation
Every technique doc so far (01-07) improves a model we train ourselves.  The
substrate is now good enough that the binding constraint is no longer the
architecture, it is the *data and compute budget of pre-training*.  Importing
removes that constraint without giving up self-hosting: inference still runs in
NeLisp/Elisp over =photon-tensor= and =nelisp-gpu=, and the model stays
self-extensible (LoRA, Coconut, recurrent depth, the agent harness in Doc 05 all
still apply on top of an imported base).

Licensing makes Qwen specifically the right donor.  The core Qwen models are
Apache-2.0, which places no restriction on the use of model outputs or on
derivative weights -- so both distillation (Section "Data") and weight
conversion are permitted.  This is *not* true of outputs taken from hosted APIs
(OpenAI, Anthropic, Gemini), whose terms forbid using outputs to train competing
models.  The rule for this repo: donors must be locally-run open weights under
Apache-2.0 or MIT, never a hosted API.

* Principle
Qwen3's dense decoder block is the block this repo already implements:
pre-norm RMSNorm, grouped-query attention with RoPE, a SwiGLU feed-forward, a
final RMSNorm, and (at the small sizes) a head tied to the embedding.  Doc 01's
Phase B established that weights do not have to be f32 to run here: the
=nelisp-gpu= kernel set already carries packed integer weights and reads them
back inside the kernel.

So import is three separate problems, and only the first is new science-free
plumbing:

1. *Get the bytes in.*  =safetensors= is a trivial container (8-byte
   little-endian header length, a JSON header of name -> dtype/shape/offsets,
   then raw tensor bytes).  Qwen3 ships bf16; bf16 -> f32 is a 16-bit shift, or
   sign/exponent/mantissa arithmetic plus =ldexp= in pure Elisp.
2. *Make them fit.*  =photon-tensor= data is a =make-vector= of boxed floats,
   about 24 bytes per parameter in Emacs.  Qwen3-0.6B would need ~14 GB and
   Qwen3-4B ~96 GB in that representation, and =photon-weights-v1= /
   =nl-llm-ckpt-v1= are =prin1= text sexps, so a 0.6B checkpoint would be a
   multi-GB text file that =read= will not finish.  Both are blockers; the fix
   is one packed binary representation used for storage *and* residency.
3. *Make them correct.*  Token ids must match the ids the donor was trained on,
   and three architecture details differ from what =nl-llm-attn.el= assumes.

* Where it lands here
Already present, no change needed:

- =:kv-heads= and =:rope-base= are already model-plist options
  (=nl-llm-model-forward=, =nl-llm-attn.el=), so GQA and Qwen3's
  =rope_theta = 1e6= are configuration, not code.
- =nl-llm-moe.el= covers a top-k sparse FFN, should a Qwen3-MoE donor be
  attempted later.
- Biases are supported throughout, so a Qwen2.5 donor's qkv bias is a superset
  case, not a gap.
- =nelisp-gpu-kernels.el= has =bitlinear-dp4a-1f=: four int8 lanes bitcast into
  one f32, per-row =GAMMA= activation quantization and a =BETA= weight scale,
  using hardware DP4A.  That is W8A8 per-channel quantization, and the GTX 1060
  is Pascal SM6.1, so DP4A is real hardware there (~17.7 TOPS).
- =nl-llm-bitnet-pack= is the template for a packer: same shape of code, base-4
  digits replaced by four int8 lanes.
- =lisp/nl-llm-lora*.el= plus the on-device training path is the adaptation
  route once a base is imported.
- =lisp/nl-llm-agent-openai.el= can point at a local Ollama instance today,
  which is how the teacher is served for distillation with zero conversion work.

Gaps, in descending order of size:

1. *Decoupled head dimension.*  =nl-llm-attn.el= derives =hd= as =(/ dim heads)=
   at every call site (=nl-llm-gqa=, =nl-llm-kv-new=, =nl-llm-decode-step=), and
   the GPU decode path inherits that assumption.  Qwen3 decouples =head_dim=
   from =hidden_size / num_heads= -- Qwen3-0.6B is hidden 1024, 16 Q heads,
   =head_dim= 128, so =q_proj= is 1024 -> 2048 and =wo= is 2048 -> 1024.  An
   explicit =:head-dim= has to be threaded through attention, the KV cache
   layout, and =nl-llm-gpu-decode.el=.  This is the largest single change and it
   touches the shapes the autograd path also relies on.
2. *No binary weight IO.*  Both existing formats are text sexps.  A new
   =nl-llm-weights-v2= binary format is required: a small sexp or JSON header
   followed by packed int8 blocks, loadable without materializing an
   intermediate float vector per tensor.
3. *QK-norm.*  SHIPPED alongside the rotation convention; see Status.  The
   forward path applies it from =:q-norm= / =:k-norm=; the autograd backward,
   needed only to train through it, is not written.
4. *Tokenizer.*  =photon-bpe= is byte-level BPE with its own merges, so it is
   structurally compatible with Qwen's vocabulary and can take its merge table
   directly.  But =photon-bpe= has no pre-tokenizer: Qwen splits text with a
   tiktoken-style regex *before* applying merges.  Importing merges alone
   produces a different id sequence than the donor was trained on, which yields
   fluent-looking garbage rather than an obvious error.  The splitter is
   mandatory and is the highest-risk correctness item in this doc.
   SHIPPED as =lisp/nl-llm-qwen-tokenizer.el=, a separate replay tokenizer
   rather than an extension of =photon-bpe=: photon-bpe learns merges from a
   corpus, this one reproduces a fixed donor table, and the two jobs want
   opposite things from the data structures.  See Status for what the splitter
   turned out to require.

* Design

** Phase 0 -- teacher available, zero conversion
Serve a Qwen3 donor locally (Ollama) and drive it through the existing
OpenAI-compatible provider adapter.  This delivers an immediately usable agent,
and more importantly it is the data generator for every later phase and the
oracle for Phase 1.  On this hardware: Qwen3-4B Q4 (~2.5 GB) fits in 6 GB VRAM;
Qwen3-30B-A3B Q4 (~18 GB) runs on the 62 GB host at usable batch speed because
only ~3B parameters are active, which makes it the better teacher for overnight
data generation.

** Phase 1 -- tokenizer parity
Import Qwen's merge table into =photon-bpe= and implement the pre-tokenizer
split.  Done before any weight work, because it fixes the vocabulary: models
trained natively in the meantime become id-compatible with a future imported
base, so the base can be dropped in underneath them.

Vocabulary pruning belongs here.  Qwen's vocab is 151,936; at dim 1024 with a
tied head, the embedding alone is ~155M parameters, about 26% of Qwen3-0.6B and
the single largest tensor.  Restricting to the tokens that actually occur in
Japanese prose plus Elisp source (~32k) reduces that to ~33M and the whole model
to ~0.48B, cutting both VRAM and the decode bandwidth that dominates latency.
Pruning is a row deletion on =wte= plus an id remap in the tokenizer, and it is
only safe once parity is proven on the unpruned table.

** Phase 2 -- import and run (the actual "no training" payoff)
Shape first, then the offline converter, then the self-hosted reader:

- *2a.* SHIPPED.  Make the decoupled head width representable, so a Qwen-shaped
  model can exist at all before any weight is read.  See Status.
- *2b.* SHIPPED.  A one-shot offline converter (Python) reads the donor safetensors,
  applies per-output-row int8 quantization, and writes =nl-llm-weights-v2=.
  Python here is an offline tool, not runtime -- the NeLisp runtime stays pure
  Elisp.  Building it first gets working numerics in days instead of weeks.
- *2c.* SHIPPED for the CPU (reader + reference forward); the resident-GPU
  upload is pending on a bytes-in primitive.  A pure-Elisp loader for
  =nl-llm-wts-v1= that uploads straight to
  resident GPU buffers via =OP_WRITE_RESIDENT=, never materializing a
  full-precision float vector on the Elisp side.
- *2d.* SHIPPED (every linear; a resident-weights pass and the head remain).
  Route the imported
  linears through a per-row-scale DP4A kernel, reuse the
  resident decode from =nl-llm-gpu-decode.el=, and migrate that path to
  =:head-dim= (it currently refuses the key rather than deriving a wrong width).
- *2e.* Port the converter's reader to Elisp once 2b-2d agree numerically; the
  Python version then survives as the parity oracle, not as a dependency.

Weight-name mapping (=model.layers.N.self_attn.q_proj.weight= -> =:wq=, and so
on) is a table in the converter, checked exhaustively: every donor tensor must
be consumed or explicitly declared ignored, so a silently dropped tensor cannot
pass as a successful conversion.

Memory budget at int8, per-channel scales:

| donor      | int8 weights | fits 6 GB VRAM        |
|------------+--------------+-----------------------|
| Qwen3-0.6B | ~0.6 GB      | yes, comfortably      |
| Qwen3-1.7B | ~1.7 GB      | yes                   |
| Qwen3-4B   | ~4.0 GB      | yes, with KV headroom |

Do *not* route an imported model through the ternary path of Doc 01.  BitNet
b1.58 assumes Phase-A QAT trained the latent weight whose ternary quantization
is the deployed weight; rounding an already-trained f32/bf16 weight to ternary
without that training collapses quality.  W8A8 per-channel is the near-lossless
option and it is the one the existing kernel already implements.

** Phase 3 -- adaptation (STARTED; see Status)
Freeze the imported base, train a LoRA (rank 8, ~2M parameters) on data
distilled from the Phase-0 teacher, using the existing completion-only
supervision in =nl-llm-agent-supervised.el= (prompt stays attention context,
loss only on completion targets) and the on-device training path.  The existing
bounds (=max-examples 128=, =max-example-tokens 4096=) already shape the
ingestion format, so this phase is mostly data plumbing plus provenance
recording in =data/SOURCE.txt=.

* Verification
Ordered so that each check can only pass for the right reason:

1. *Tokenizer parity, before anything else.*  Feed a corpus (Japanese prose,
   Elisp source, mixed CJK/ASCII, emoji, lone surrogates and other malformed
   UTF-8) to both the donor tokenizer and =photon-bpe=, and require *identical*
   id sequences.  A negative control is mandatory here: perturb one merge rank
   and confirm the check goes red.  Without that control the comparison can
   pass by comparing two identically-broken paths.
2. *Round-trip.*  =decode(encode(s)) == s= for every input above, which
   =photon-bpe='s byte-level base alphabet guarantees by construction and the
   pre-tokenizer must not break.
3. *Dequantization.*  Packed int8 unpacked back to f32 must match the
   quantizer's own output exactly (bit-level), separating packing bugs from
   quantization error.
4. *Layer-by-layer against a reference.*  Run the donor under a reference
   implementation with a fixed prompt, dump per-layer hidden states, and compare
   the Elisp forward layer by layer at f32 (relative L2, threshold per layer).
   Compare the *first diverging layer*, not just the logits -- a single
   transposed weight or a wrong =head_dim= shows up as a specific layer, whereas
   the logits only say "wrong".
5. *Quantization loss, stated not assumed.*  Perplexity of the imported model at
   f32 vs W8A8 on a held-out slice, reported as a number in the doc's Status
   section.  An import that silently costs 3 perplexity points is not a success.
6. *Greedy determinism.*  Same prompt, same seed, same output across runs and
   across the CPU reference and GPU decode paths -- the same equivalence gate
   Doc 04 used.

* Cost / risk
- Effort: Phase 0 trivial.  Phase 1 medium (the pre-tokenizer regex is fiddly in
  Emacs regexp syntax and needed a hand-rolled splitter).  Phase 2a came in
  smaller than estimated; the remaining weight of Phase 2 is the loader and the
  DP4A routing, not the shapes.
  Phase 3 low, the machinery exists.
- Risk: the tokenizer failure mode is silent.  Mitigation is check 1 with its
  negative control, run before any weight is converted.
- Risk: the =head_dim= refactor touches shapes that =nl-llm-autograd.el= and the
  GPU autograd builder depend on, so it can regress training paths that have
  nothing to do with import.  Mitigation: land it as a standalone change with
  the existing gradcheck and decode suites green, *before* the loader, and keep
  =(/ dim heads)= as the default when =:head-dim= is absent so every current
  model and test is unaffected.
- Risk: dispatch overhead, not arithmetic, sets decode latency.  28 layers at
  ~7 kernels each is ~200 dispatches per token; at 0.5 ms of Elisp-side overhead
  per dispatch that is ~100 ms/token against a ~3 ms/token weight-bandwidth floor
  (0.6 GB at ~197 GB/s).  Mitigation: record the whole decode step as one
  compiled batch (=OP_COMPILE= / =OP_RUN_COMPILED=, the "compiled batches" path
  already used by the on-device training builder) instead of dispatching
  per-layer.
- Risk: license hygiene on redistribution.  Personal use is unencumbered, but
  publishing converted weights requires carrying the donor's Apache-2.0 LICENSE
  and NOTICE and stating what was changed.  Record the donor revision hash in
  the =nl-llm-weights-v2= header so a published artifact is traceable.

* Open questions
- Donor choice: Qwen3 (needs QK-norm and decoupled =head_dim=) versus Qwen2.5
  (needs neither, has qkv bias which is already supported, but is an older
  model).  Starting at Qwen2.5 would shrink Phase 2 considerably and prove the
  pipeline before taking on the refactor.
- Which tensors to leave at f32: embedding/tied head, and the RMSNorm gains,
  are small and sensitive; quantizing them may cost more accuracy than VRAM it
  saves.  Doc 01 left the same question open.
- Whether vocabulary pruning should be part of the shipped conversion or an
  optional post-pass.  It changes the model's id space, so a pruned artifact is
  not interchangeable with the donor's tokenizer.
- Whether the KV cache should also be int8.  Doc 02's bounded window already
  caps its size, so this may not be worth the accuracy risk at these scales.
- Qwen3-MoE (30B-A3B) is out of reach for 6 GB VRAM even at int8, but only ~3B
  parameters are active per token.  Expert streaming from the 62 GB host is
  possible in principle and is deliberately out of scope here.
