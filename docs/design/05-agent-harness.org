#+TITLE: nelisp-llm Design 05 -- Self-hosting agent harness
#+OPTIONS: toc:nil num:nil

* Status
Phase 1 (harness) SHIPPED -- =lisp/nl-llm-agent.el= + =test/agent-test.el= (11/11)
+ =examples/agent-demo.el=.
Phase 4 (real model wired as policy, via constrained decoding) SHIPPED --
=lisp/nl-llm-agent-model.el= + =test/agent-model-test.el= (6/6) +
=examples/agent-model-demo.el=.
Phase 5 (self-improvement loop) SHIPPED -- =lisp/nl-llm-agent-improve.el= +
=test/agent-improve-test.el= (4/4) + =examples/agent-improve-demo.el=: a model
trains on its OWN successful actions and its success rate climbs 10% -> 100%.
Phase 5 with a REAL coding reward SHIPPED -- =test/agent-code-test.el= (5/5) +
=examples/agent-code-demo.el=: the model synthesises a function and the reward
RUNS it against tests; code-synthesis success climbs from the ~1/9 baseline to
100% on execution feedback alone.
Phase 2 (sandboxing + permission gating) SHIPPED -- =test/agent-sandbox-test.el=
\(11/11) + =examples/agent-sandbox-demo.el=: untrusted Elisp runs in an isolated
subprocess with a timeout, and destructive forms / shells / out-of-workdir edits
are refused before they run.  (Fuzzy edit application is the remaining P2 item.)
Phase 3 below is designed, not built.
Service provider layer SHIPPED -- =lisp/nl-llm-agent-provider.el= +
=test/agent-provider-test.el= (20/20) and the native adapter in
=nl-llm-agent-model.el= + =test/agent-native-provider-test.el= (4/4).  A live
session can change provider/model without replacing the agent loop; failed opens
leave the previous backend active.
OpenAI-compatible adapter SHIPPED -- =lisp/nl-llm-agent-openai.el= +
=test/agent-openai-provider-test.el= (10/10).  Request translation and response
extraction run on Emacs and standalone NeLisp with an injected transport; the
default live HTTP transport uses Emacs URL and is pinned without network access
by =test/agent-openai-http-test.el= (4/4).
Transactional evolution and service artifacts SHIPPED -- =lisp/nl-llm-evolve.el=
+ =test/evolve-test.el= (13/13), and =lisp/nl-llm-agent-artifact.el= +
=test/agent-artifact-test.el= (14/14).  A promotion publishes an immutable,
digest-addressed checkpoint and atomically updates its data-only catalog before
the champion changes.  The PAV exporter preserves the independently trained
output head and matches training-model logits before real constrained inference.
The sibling =nelisp-agent= service discovers a later generation and switches to
it without restarting its standalone NeLisp worker.
Evaluated improvement queue SHIPPED -- =lisp/nl-llm-evolve-queue.el= +
=test/evolve-queue-test.el= (28/28 on Emacs and standalone NeLisp), with the real
P5 composition in =lisp/nl-llm-agent-evolve.el= + =test/agent-evolve-test.el=
(5/5).  Model output supplies bounded trajectory data only; trusted code chooses
the CPU or GPU trainer and fixed benchmark.  GPU-resident challenger training is
SHIPPED through the same data protocol: =test/agent-evolve-gpu-test.el= (7/7)
trains the isolated copy in a dedicated compiled nlga graph and proves the old
champion is unchanged before immutable publication.  With recovery disabled it
reads back once; with recovery enabled bounded interval readbacks persist the
weights, Adam m/v, optimizer step, and completed steps.  The test interrupts a
lightweight job after 4 of 8 Adam steps and explicit resume matches continuous
training exactly (maxdiff 0).  The =nelisp-agent= host exposes the queue
through approval-gated typed tools.  Its cross-repository capstone pins one live
run from proposal through training, evaluation, immutable publication, separately
approved activation, catalog refresh, and the next inference on the new model.
Private atomic queue checkpoints restore pending/audited jobs; an in-flight job
becomes =interrupted= rather than being replayed.  A separate private training
checkpoint is bound to the exact job, proposal, trusted configuration, parent,
optimizer, and plan; only explicit =model.improvement.resume= can consume it.
Interrupted jobs survive completed-history trimming, including zero retention,
and consume the same admission budget as pending and running jobs.  Explicit
cancellation releases the slot and cleans recovery state after durable queue
persistence.  Resume and restore validate outstanding data against the current
trusted handler before execution; cancellation can discard a revoked proposal.
Promoted PAV artifacts reload as detached trainable champions and retain
monotonic generation numbering.  The sibling CLI assembles those boundaries
from bounded JSON without configurable callbacks.  Its optional autonomous
scope pre-authorizes only queue mutations and activation of
=PROVIDER/ID-PREFIX-gN=; unrelated tools and remote switches remain denied, and
each grant is audited separately from human approval.

* Why
An LLM by itself only emits text.  What lets Claude Code / Codex *act* -- edit
files, run code, iterate -- is the scaffold around the model, not the model.  This
design distils the parts that make the open-source coding agents work and
reimplements them in Elisp, so an nelisp-llm-generated model can be given agent
capabilities and -- because the action language is Lisp running inside Emacs --
extend, and ultimately retrain, itself.

The agent loop is ~100 lines; the leverage is in five components.  Each is taken
from the best open-source exemplar and adapted to the Emacs/NeLisp substrate.

* The five components (concept + application)

** 1. Loop + linear history  (from mini-swe-agent)
mini-swe-agent gets >74% on SWE-bench Verified with a ~100-line loop over a
*completely linear* message history, each action executed independently, and *no
tool-calling API* -- the model drives it with plain text.  That model-agnosticism
is exactly what a small nelisp-llm model needs (small models emit JSON tool calls
unreliably).
Application: =nl-llm-agent-run TASK POLICY= where POLICY is just =(MESSAGES ->
text)=.  Linear history; one action per turn; stop on DONE or step cap.  The same
linear trajectory is later training data (Phase 5).

** 2. Action language = Elisp  (from smolagents CodeAct)
smolagents has the model write its actions *as code* rather than as tool-call
JSON.  In Emacs the natural action code is Elisp, evaluated in-process -- so every
Emacs and anvil primitive is already a tool, and the agent extends itself by
writing more Elisp.  This is the crux of the self-improvement story.
Application: a ```elisp block is read and =eval='d; value or error becomes the
observation.  (Production sandboxes via =emacs --batch=, Phase 2.)

** 3. Edit format = SEARCH/REPLACE  (from Aider)
Whole-file rewrites are unreliable for small models; Aider's diff format has the
model emit only the changed span as a SEARCH/REPLACE block.
Application: =nl-llm-agent--apply-edit= matches the SEARCH text exactly (must be
unique), splices in REPLACE.  (Aider's fuzzy fallbacks -- diff-match-patch, git
cherry-pick -- are Phase 2.)

** 4. Guardrails = linter on edit  (from SWE-agent ACI)
SWE-agent's biggest ACI win: run a linter on every edit and *discard* it if the
result does not parse, feeding the error back so the agent retries.  This is what
makes a weak model usable.
Application: an =*.el= edit whose result fails =check-parens= is rejected and the
error returned; the file is left untouched.  (Pinned by the test: a deliberately
unbalanced edit is rejected and the file still parses.)

** 5. Repo map = ranked structure  (from Aider)
Aider feeds the model a ranked map of the codebase (tree-sitter tags + PageRank)
instead of whole files -- essential for a small context window (and a natural fit
with our StreamingLLM sink+window decode).
Application: Phase 1 ships a cheap =nl-llm-agent-repo-map= (files + top-level
def names via regexp).  Phase 3 upgrades it to Emacs 29 built-in tree-sitter +
a PageRank over the call graph.

* Self-improvement loop (the goal)
#+begin_example
  nelisp-llm model --(policy)--> agent harness --(acts: edit/eval/test)-->
  outcome (tests pass?) --(reward/filter)--> trajectories --(nlga fine-tune)-->
  better nelisp-llm model  --> repeat
#+end_example
The harness turns the model into an agent; the agent's successful trajectories
(linear histories, by design) are training data; nlga (the on-device autograd +
Adam already shipped) fine-tunes the model on its own agentic behaviour.  Each
turn the model gets better at using -- and improving -- itself.

* Phasing
- *P1 (SHIPPED)*: harness -- loop, Elisp/edit/shell/done actions, SEARCH/REPLACE +
  linter guardrail, scripted-policy tests, cheap repo map.
- *P2 (SHIPPED, fuzzy edits pending)*: sandboxing + permission gating.  Untrusted
  Elisp runs in an isolated =emacs -Q --batch= subprocess with a wall-clock
  timeout (=nl-llm-agent--eval-elisp-sandbox=: no hang, no host-state corruption,
  crashes contained).  A permission set (=nl-llm-agent-safe-permissions=) refuses,
  before anything runs, destructive forms (denylist), shells, and edits resolving
  outside the workdir.  Default stays permissive (in-process) for trusted/test use.
  Remaining: fuzzy SEARCH/REPLACE application (diff-match-patch-style).
- *P3*: tree-sitter repo map + PageRank ranking; feed only ranked context.
- *P4 (SHIPPED)*: nelisp-llm generation wired as the POLICY.  A char tokenizer
  (vocab 96) + a CONSTRAINED decoder: a grammar says which characters are legal at
  each position, the model's logits are masked to that set and the argmax taken;
  fixed scaffold characters are forced, only variable slots are model-chosen.  So
  the output is ALWAYS a parseable, executable action however weak the model --
  the model picks content, the grammar guarantees structure.  Generation engine is
  =nl-llm-decode-step= (CPU) behind a (token -> logits) STEP-FN, so the GPU /
  integrated decode swaps in unchanged.  =examples/agent-model-demo.el=: an
  untrained random model yields 3/3 structurally valid Elisp actions (gibberish
  content -> P5).
- *P5 (SHIPPED)*: the self-improvement loop.  Each round the model samples K
  actions under the grammar (constrained SAMPLING), a reward keeps the successes,
  and the model is fine-tuned on its own winning trajectories by next-char CE
  (real backprop, photon-autograd + nl-llm-autograd, in-place weight updates).
  =examples/agent-improve-demo.el=: success rate climbs 10% -> 18% -> 52% -> 92%
  -> 100% with no external labels -- expert-iteration / STaR.  The reward is now
  EXECUTION-BASED: the model synthesises =(defun f235 (x) (OP x N))= by choosing
  the operator and constant under a multi-slot grammar
  (=nl-llm-agent-grammar-template=), and the reward defines the function and runs
  it against test cases (f(2)=6, f(5)=15; unique solution =(* x 3)=) -- so the
  model learns to write code that PASSES TESTS from execution feedback alone
  (=test/agent-code-test.el=, =examples/agent-code-demo.el=, ~1/9 -> 100%).  The
  same loop with richer grammars + bigger test suites is how the model improves
  its real coding ability; nlga (GPU) swaps in for the CPU autograd at scale.
  Richer reward SHIPPED: a multi-task curriculum (=nl-llm-agent-improve-tasks=),
  each task a (spec-prompt . multi-test-case reward) -- multiple cases are what pin
  the right program (one case can't tell =(* x 3)= from =(+ x 4)= at x=2).  The
  model is CONDITIONED on each spec (the prompt is fed as context) and must read it
  to synthesise the matching function; a 2-block model (depth needed to copy the
  spec into the code) trained with a REPLAY BUFFER (keeps the curriculum balanced)
  lifts mean success across the bank from the ~1/8 baseline
  (=test/agent-tasks-test.el=, =examples/agent-tasks-demo.el=: 15% -> 56% over the
  4-task bank; a tiny char model only partially generalises the conditional copy --
  capacity/scale is exactly what P5-on-GPU buys).  GPU scale SHIPPED: the
  fine-tune step runs on-device via nlga -- =test/agent-gpu-test.el= +
  =examples/agent-gpu-finetune-demo.el= train an nlga model on the agent's
  successful trajectories (CE + Adam, the same engine behind train-*-gpu) and the
  trajectory CE drops 4.6 -> 1.8 on the GPU.  The loop is then CLOSED on-device
  (=lisp/nl-llm-agent-ondevice.el=): one model, two views sharing weight tensors --
  a CPU view (stacked =nl-llm-ag-block=) rolls out, an nlga GPU graph built from the
  SAME host tensors trains, and =nlga-readback= copies the trained weights straight
  back into the shared tensors (the two are the same function -- CPU vs nlga logits
  agree to ~2e-5 -- so the transfer is just the readback).  Loop: rollout (CPU) ->
  reward -> train (GPU) -> readback -> repeat.  =test/agent-ondevice-test.el= +
  =examples/agent-ondevice-demo.el=: the CPU-rollout success rises from the 25%
  random baseline to a ~70% peak with the head weight provably changed only by the
  GPU training (a replay buffer stabilises the STaR step).  Whole loop -- generate,
  act, reward, learn -- runs in nelisp, on the GPU.
- *P6 (SHIPPED, core)*: provider registry + provider-neutral session policy.  A
  provider supplies a public model catalog and open / complete / close callbacks.
  Switching opens the challenger backend before closing the current one, retains
  provider-neutral messages, rejects ambiguous model ids, and records an audit
  event.  The native adapter wraps =nl-llm-agent-model-policy=; OpenAI-compatible
  request/response translation and an Emacs URL transport are also shipped.  The
  promoted-artifact provider adds lazy, digest-verified checkpoint loading and a
  live catalog; the sibling =nelisp-agent= host brokers that catalog into a running
  standalone NeLisp worker.  Its guarded improvement tools let that worker submit
  inert trajectory experiments and request trusted evaluation without receiving
  trainer or evaluator callbacks.  An independently approved service directive
  performs the transactional model switch in place.  Native NeLisp HTTP remains
  service-layer work.

* Sources
mini-swe-agent (github.com/SWE-agent/mini-swe-agent); SWE-agent ACI
(swe-agent.com, arXiv:2405.15793); Aider edit-formats + repo map (aider.chat);
smolagents CodeAgent (huggingface.co/docs/smolagents); OpenHands; goose.
